using Andy.X.Streams.Abstractions;

namespace Andy.X.Streams
{
    public abstract class Flow<TInput, TOutput> where TInput : IFlowRequest where TOutput : IFlowResponse
    {
        // todo ...
    }
}
